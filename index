<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SIGNAL — Alertes Marchés Intelligentes</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=IBM+Plex+Mono:wght@300;400;500&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #080c10;
    --surface: #0e1419;
    --surface2: #141c24;
    --border: rgba(255,255,255,0.07);
    --accent: #00e5a0;
    --accent2: #0af;
    --warn: #ff6b35;
    --text: #e8edf2;
    --muted: #5a6a7a;
    --font-display: 'Syne', sans-serif;
    --font-mono: 'IBM Plex Mono', monospace;
    --font-serif: 'Instrument Serif', serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-display);
    overflow-x: hidden;
    line-height: 1.6;
  }

  /* NOISE  OVERLAY */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 0;
    opacity: 0.4;
  }

  /* ====== NAV ====== */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 48px;
    border-bottom: 1px solid var(--border);
    backdrop-filter: blur(20px);
    background: rgba(8,12,16,0.7);
  }

  .logo {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 20px;
    letter-spacing: 0.15em;
    color: var(--accent);
    text-transform: uppercase;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .logo-dot {
    width: 8px; height: 8px;
    background: var(--accent);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.7); }
  }

  .nav-links {
    display: flex;
    gap: 36px;
    list-style: none;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .nav-links a {
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--text); }

  .nav-cta {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .btn-ghost {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--muted);
    background: none;
    border: none;
    cursor: pointer;
    transition: color 0.2s;
  }

  .btn-ghost:hover { color: var(--text); }

  .btn-primary {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--bg);
    background: var(--accent);
    border: none;
    padding: 10px 22px;
    cursor: pointer;
    transition: all 0.2s;
    clip-path: polygon(8px 0%, 100% 0%, calc(100% - 8px) 100%, 0% 100%);
  }

  .btn-primary:hover {
    background: #00ffc2;
    transform: translateY(-1px);
  }

  /* ====== HERO ====== */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 120px 48px 80px;
    position: relative;
    overflow: hidden;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,229,160,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,229,160,0.04) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 50%, black 30%, transparent 100%);
  }

  .hero-glow {
    position: absolute;
    width: 600px;
    height: 600px;
    background: radial-gradient(ellipse, rgba(0,229,160,0.08) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(0,229,160,0.08);
    border: 1px solid rgba(0,229,160,0.2);
    padding: 6px 14px;
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    color: var(--accent);
    text-transform: uppercase;
    margin-bottom: 32px;
    width: fit-content;
  }

  .hero-badge span {
    width: 6px; height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: pulse 1.5s ease-in-out infinite;
  }

  .hero h1 {
    font-family: var(--font-display);
    font-size: clamp(52px, 8vw, 108px);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.03em;
    text-transform: uppercase;
    max-width: 900px;
    position: relative;
    z-index: 1;
  }

  .hero h1 em {
    font-family: var(--font-serif);
    font-style: italic;
    font-weight: 400;
    color: var(--accent);
    text-transform: none;
    letter-spacing: -0.01em;
  }

  .hero-sub {
    margin-top: 32px;
    font-family: var(--font-mono);
    font-size: 15px;
    color: var(--muted);
    max-width: 540px;
    line-height: 1.8;
    position: relative;
    z-index: 1;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-top: 48px;
    position: relative;
    z-index: 1;
  }

  .btn-hero {
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--bg);
    background: var(--accent);
    border: none;
    padding: 16px 36px;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 10px;
    clip-path: polygon(12px 0%, 100% 0%, calc(100% - 12px) 100%, 0% 100%);
    transition: all 0.25s;
  }

  .btn-hero:hover {
    background: #00ffc2;
    transform: translateY(-2px);
    box-shadow: 0 20px 40px rgba(0,229,160,0.2);
  }

  .btn-hero-outline {
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text);
    background: transparent;
    border: 1px solid var(--border);
    padding: 16px 36px;
    cursor: pointer;
    transition: all 0.25s;
  }

  .btn-hero-outline:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .hero-stats {
    display: flex;
    gap: 48px;
    margin-top: 80px;
    padding-top: 48px;
    border-top: 1px solid var(--border);
    position: relative;
    z-index: 1;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .stat-num {
    font-family: var(--font-display);
    font-size: 32px;
    font-weight: 800;
    color: var(--text);
    letter-spacing: -0.02em;
  }

  .stat-num span { color: var(--accent); }

  .stat-label {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* ====== TICKER ====== */
  .ticker-wrap {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    overflow: hidden;
    padding: 14px 0;
  }

  .ticker {
    display: flex;
    gap: 0;
    animation: ticker 25s linear infinite;
    white-space: nowrap;
  }

  .ticker-item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 0 32px;
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.05em;
    border-right: 1px solid var(--border);
  }

  .ticker-sym { color: var(--muted); }
  .ticker-price { color: var(--text); font-weight: 500; }
  .ticker-up { color: var(--accent); }
  .ticker-down { color: var(--warn); }

  @keyframes ticker {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* ====== LIVE ALERTS DEMO ====== */
  .section {
    padding: 120px 48px;
    position: relative;
  }

  .section-label {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 16px;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(36px, 5vw, 64px);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -0.03em;
    text-transform: uppercase;
    margin-bottom: 20px;
  }

  .section-title em {
    font-family: var(--font-serif);
    font-style: italic;
    font-weight: 400;
    color: var(--accent);
    text-transform: none;
  }

  .alerts-demo {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
    margin-top: 64px;
    background: var(--border);
  }

  .alert-card {
    background: var(--surface);
    padding: 28px 32px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    position: relative;
    overflow: hidden;
    transition: background 0.3s;
  }

  .alert-card:hover { background: var(--surface2); }

  .alert-card::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 3px;
  }

  .alert-card.green::before { background: var(--accent); }
  .alert-card.blue::before { background: var(--accent2); }
  .alert-card.orange::before { background: var(--warn); }
  .alert-card.yellow::before { background: #f5c542; }

  .alert-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .alert-type {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 4px 10px;
    border-radius: 2px;
  }

  .type-smart { background: rgba(0,229,160,0.12); color: var(--accent); }
  .type-tech { background: rgba(0,170,255,0.12); color: var(--accent2); }
  .type-price { background: rgba(255,107,53,0.12); color: var(--warn); }
  .type-news { background: rgba(245,197,66,0.12); color: #f5c542; }

  .alert-time {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
  }

  .alert-symbol {
    font-family: var(--font-display);
    font-size: 28px;
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--text);
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .alert-symbol small {
    font-size: 14px;
    font-weight: 400;
    color: var(--muted);
  }

  .alert-message {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text);
    line-height: 1.5;
  }

  .alert-value {
    font-size: 24px;
    font-weight: 700;
    font-family: var(--font-display);
  }

  .val-green { color: var(--accent); }
  .val-red { color: var(--warn); }

  /* Animated dot */
  .live-dot {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--accent);
  }

  .live-dot::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: pulse 1.5s ease-in-out infinite;
  }

  /* ====== HOW IT WORKS ====== */
  .steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2px;
    margin-top: 64px;
    background: var(--border);
  }

  .step {
    background: var(--surface);
    padding: 40px 32px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    position: relative;
    overflow: hidden;
  }

  .step-num {
    font-family: var(--font-mono);
    font-size: 60px;
    font-weight: 300;
    color: rgba(255,255,255,0.04);
    line-height: 1;
    position: absolute;
    top: 20px;
    right: 20px;
  }

  .step-icon {
    width: 48px;
    height: 48px;
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
  }

  .step h3 {
    font-family: var(--font-display);
    font-size: 18px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -0.01em;
  }

  .step p {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--muted);
    line-height: 1.7;
  }

  /* ====== PRICING ====== */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr 1.1fr 1fr;
    gap: 2px;
    margin-top: 64px;
    background: var(--border);
  }

  .plan {
    background: var(--surface);
    padding: 48px 36px;
    display: flex;
    flex-direction: column;
    gap: 28px;
    position: relative;
    transition: background 0.3s;
  }

  .plan:hover { background: var(--surface2); }

  .plan.featured {
    background: var(--surface2);
    outline: 2px solid var(--accent);
    outline-offset: -2px;
  }

  .plan-badge {
    position: absolute;
    top: -1px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--accent);
    color: var(--bg);
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 4px 14px;
  }

  .plan-name {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .plan-price {
    display: flex;
    align-items: baseline;
    gap: 4px;
  }

  .plan-price .amount {
    font-family: var(--font-display);
    font-size: 56px;
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 1;
    color: var(--text);
  }

  .plan-price .currency {
    font-family: var(--font-mono);
    font-size: 20px;
    color: var(--muted);
    align-self: flex-start;
    margin-top: 10px;
  }

  .plan-price .period {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--muted);
  }

  .plan-desc {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--muted);
    line-height: 1.7;
    padding-top: 4px;
    border-top: 1px solid var(--border);
  }

  .plan-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 12px;
    flex: 1;
  }

  .plan-features li {
    display: flex;
    align-items: center;
    gap: 12px;
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text);
  }

  .plan-features li.disabled { color: var(--muted); }

  .feat-check {
    width: 20px;
    height: 20px;
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 11px;
  }

  .plan.featured .feat-check { border-color: var(--accent); color: var(--accent); }

  .btn-plan {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 14px;
    cursor: pointer;
    transition: all 0.25s;
    clip-path: polygon(10px 0%, 100% 0%, calc(100% - 10px) 100%, 0% 100%);
    text-align: center;
  }

  .btn-plan-outline {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--text);
  }

  .btn-plan-outline:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .btn-plan-filled {
    background: var(--accent);
    border: none;
    color: var(--bg);
  }

  .btn-plan-filled:hover {
    background: #00ffc2;
    transform: translateY(-2px);
    box-shadow: 0 16px 32px rgba(0,229,160,0.2);
  }

  /* ====== CHANNELS ====== */
  .channels {
    display: flex;
    gap: 2px;
    margin-top: 64px;
    background: var(--border);
  }

  .channel {
    flex: 1;
    background: var(--surface);
    padding: 36px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    text-align: center;
    transition: background 0.3s;
  }

  .channel:hover { background: var(--surface2); }

  .channel-icon {
    font-size: 32px;
    margin-bottom: 4px;
  }

  .channel-name {
    font-family: var(--font-display);
    font-size: 15px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .channel-desc {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--muted);
    line-height: 1.6;
  }

  /* ====== STRATEGY CARDS ====== */
  .strategies {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    margin-top: 64px;
    background: var(--border);
  }

  .strategy-card {
    background: var(--surface);
    padding: 40px 36px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    cursor: pointer;
    transition: background 0.3s;
    position: relative;
    overflow: hidden;
  }

  .strategy-card:hover { background: var(--surface2); }

  .strategy-card::after {
    content: '→';
    position: absolute;
    bottom: 24px;
    right: 28px;
    font-size: 20px;
    color: var(--muted);
    transition: all 0.3s;
    font-family: var(--font-display);
  }

  .strategy-card:hover::after {
    color: var(--accent);
    right: 20px;
  }

  .strategy-tag {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--accent);
    background: rgba(0,229,160,0.1);
    padding: 4px 10px;
    width: fit-content;
  }

  .strategy-card h3 {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .strategy-card p {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--muted);
    line-height: 1.7;
  }

  .strategy-signals {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 4px;
  }

  .signal-chip {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    border: 1px solid var(--border);
    padding: 3px 8px;
  }

  /* ====== CTA SECTION ====== */
  .cta-section {
    padding: 120px 48px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 50% at 50% 50%, rgba(0,229,160,0.06) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-section .section-title {
    font-size: clamp(48px, 6vw, 88px);
    margin-bottom: 24px;
  }

  .cta-desc {
    font-family: var(--font-mono);
    font-size: 15px;
    color: var(--muted);
    margin-bottom: 48px;
    max-width: 480px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.8;
  }

  /* ====== FOOTER ====== */
  footer {
    border-top: 1px solid var(--border);
    padding: 40px 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .footer-logo {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 16px;
    letter-spacing: 0.15em;
    color: var(--accent);
    text-transform: uppercase;
  }

  .footer-links {
    display: flex;
    gap: 32px;
    list-style: none;
  }

  .footer-links a {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--muted);
    text-decoration: none;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: color 0.2s;
  }

  .footer-links a:hover { color: var(--text); }

  .footer-copy {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 0.08em;
  }

  /* ====== ANIMATE ON SCROLL ====== */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ====== SEPARATOR ====== */
  .sep {
    height: 1px;
    background: var(--border);
    margin: 0 48px;
  }

  /* ====== MOBILE ====== */
  @media (max-width: 900px) {
    nav { padding: 16px 24px; }
    .nav-links { display: none; }
    .hero { padding: 100px 24px 60px; }
    .hero-stats { flex-wrap: wrap; gap: 28px; }
    .section { padding: 80px 24px; }
    .alerts-demo { grid-template-columns: 1fr; }
    .steps { grid-template-columns: 1fr 1fr; }
    .pricing-grid { grid-template-columns: 1fr; }
    .strategies { grid-template-columns: 1fr; }
    .channels { flex-direction: column; }
    footer { flex-direction: column; gap: 20px; text-align: center; }
    .footer-links { flex-wrap: wrap; justify-content: center; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">
    <div class="logo-dot"></div>
    Signal
  </div>
  <ul class="nav-links">
    <li><a href="#alerts">Alertes</a></li>
    <li><a href="#how">Fonctionnement</a></li>
    <li><a href="#pricing">Tarifs</a></li>
    <li><a href="#strategies">Stratégies</a></li>
  </ul>
  <div class="nav-cta">
    <button class="btn-ghost">Connexion</button>
    <button class="btn-primary">Essai gratuit</button>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-glow"></div>

  <div class="hero-badge">
    <span></span>
    Surveillance en temps réel • 24/7
  </div>

  <h1>
    Arrêtez de<br>
    surveiller,<br>
    commencez à<br>
    <em>trader.</em>
  </h1>

  <p class="hero-sub">
    Des alertes intelligentes sur les marchés financiers.<br>
    Prix, indicateurs techniques, breakouts — Signal vous alerte<br>
    au bon moment. Pas avant. Pas après.
  </p>

  <div class="hero-actions">
    <button class="btn-hero">
      Démarrer gratuitement
      <span>→</span>
    </button>
    <button class="btn-hero-outline">Voir une démo</button>
  </div>

  <div class="hero-stats">
    <div class="stat-item">
      <div class="stat-num">12<span>k</span></div>
      <div class="stat-label">Investisseurs actifs</div>
    </div>
    <div class="stat-item">
      <div class="stat-num">2<span>M+</span></div>
      <div class="stat-label">Alertes envoyées</div>
    </div>
    <div class="stat-item">
      <div class="stat-num">&lt;1<span>s</span></div>
      <div class="stat-label">Délai d'alerte</div>
    </div>
    <div class="stat-item">
      <div class="stat-num">5<span>k</span></div>
      <div class="stat-label">Actifs surveillés</div>
    </div>
  </div>
</section>

<!-- TICKER -->
<div class="ticker-wrap">
  <div class="ticker" id="ticker">
    <div class="ticker-item"><span class="ticker-sym">AAPL</span><span class="ticker-price">189.45</span><span class="ticker-up">+1.2%</span></div>
    <div class="ticker-item"><span class="ticker-sym">NVDA</span><span class="ticker-price">875.20</span><span class="ticker-up">+3.8%</span></div>
    <div class="ticker-item"><span class="ticker-sym">BTC</span><span class="ticker-price">67,420</span><span class="ticker-up">+2.1%</span></div>
    <div class="ticker-item"><span class="ticker-sym">TSLA</span><span class="ticker-price">248.60</span><span class="ticker-down">-0.9%</span></div>
    <div class="ticker-item"><span class="ticker-sym">ETH</span><span class="ticker-price">3,841</span><span class="ticker-up">+1.7%</span></div>
    <div class="ticker-item"><span class="ticker-sym">META</span><span class="ticker-price">512.30</span><span class="ticker-up">+0.5%</span></div>
    <div class="ticker-item"><span class="ticker-sym">AMZN</span><span class="ticker-price">182.10</span><span class="ticker-down">-1.4%</span></div>
    <div class="ticker-item"><span class="ticker-sym">MSFT</span><span class="ticker-price">415.80</span><span class="ticker-up">+0.8%</span></div>
    <div class="ticker-item"><span class="ticker-sym">SOL</span><span class="ticker-price">148.50</span><span class="ticker-up">+4.2%</span></div>
    <div class="ticker-item"><span class="ticker-sym">GOOGL</span><span class="ticker-price">163.90</span><span class="ticker-up">+1.1%</span></div>
    <!-- Duplicate for seamless loop -->
    <div class="ticker-item"><span class="ticker-sym">AAPL</span><span class="ticker-price">189.45</span><span class="ticker-up">+1.2%</span></div>
    <div class="ticker-item"><span class="ticker-sym">NVDA</span><span class="ticker-price">875.20</span><span class="ticker-up">+3.8%</span></div>
    <div class="ticker-item"><span class="ticker-sym">BTC</span><span class="ticker-price">67,420</span><span class="ticker-up">+2.1%</span></div>
    <div class="ticker-item"><span class="ticker-sym">TSLA</span><span class="ticker-price">248.60</span><span class="ticker-down">-0.9%</span></div>
    <div class="ticker-item"><span class="ticker-sym">ETH</span><span class="ticker-price">3,841</span><span class="ticker-up">+1.7%</span></div>
    <div class="ticker-item"><span class="ticker-sym">META</span><span class="ticker-price">512.30</span><span class="ticker-up">+0.5%</span></div>
    <div class="ticker-item"><span class="ticker-sym">AMZN</span><span class="ticker-price">182.10</span><span class="ticker-down">-1.4%</span></div>
    <div class="ticker-item"><span class="ticker-sym">MSFT</span><span class="ticker-price">415.80</span><span class="ticker-up">+0.8%</span></div>
    <div class="ticker-item"><span class="ticker-sym">SOL</span><span class="ticker-price">148.50</span><span class="ticker-up">+4.2%</span></div>
    <div class="ticker-item"><span class="ticker-sym">GOOGL</span><span class="ticker-price">163.90</span><span class="ticker-up">+1.1%</span></div>
  </div>
</div>

<!-- ALERTS DEMO -->
<section class="section" id="alerts">
  <div class="section-label reveal">Types d'alertes</div>
  <div class="section-title reveal">Chaque signal<br><em>au bon moment</em></div>

  <div class="alerts-demo reveal">
    <!-- Smart Alert -->
    <div class="alert-card green">
      <div class="alert-top">
        <span class="alert-type type-smart">🧠 Alerte Intelligente</span>
        <span class="alert-time live-dot">LIVE</span>
      </div>
      <div class="alert-symbol">NVDA <small>NASDAQ</small></div>
      <div class="alert-message">Signal d'achat fort détecté — Breakout de résistance confirmé avec volume ×2.8. RSI en zone neutre (54). EMA 20 croise EMA 50 à la hausse.</div>
      <div class="alert-value val-green">+3.8% ↑ $875.20</div>
    </div>

    <!-- Technical Alert -->
    <div class="alert-card blue">
      <div class="alert-top">
        <span class="alert-type type-tech">📊 Indicateur Technique</span>
        <span class="alert-time">14:32:07</span>
      </div>
      <div class="alert-symbol">BTC <small>CRYPTO</small></div>
      <div class="alert-message">RSI en zone de surachat (72.4). Résistance clé à $68,500. Volume en hausse de 145% sur 1h. Divergence baissière détectée sur MACD.</div>
      <div class="alert-value val-red">RSI 72.4 ⚠</div>
    </div>

    <!-- Price Alert -->
    <div class="alert-card orange">
      <div class="alert-top">
        <span class="alert-type type-price">💰 Alerte Prix</span>
        <span class="alert-time">13:15:44</span>
      </div>
      <div class="alert-symbol">TSLA <small>NASDAQ</small></div>
      <div class="alert-message">Seuil de prix atteint. TSLA passe sous les $250.00 — votre niveau de support défini. Variation journalière : -0.9%.</div>
      <div class="alert-value val-red">$248.60 ↓</div>
    </div>

    <!-- News Alert -->
    <div class="alert-card yellow">
      <div class="alert-top">
        <span class="alert-type type-news">📰 Actualité Marché</span>
        <span class="alert-time">12:00:01</span>
      </div>
      <div class="alert-symbol">FED <small>MACRO</small></div>
      <div class="alert-message">Décision Fed imminente — Taux d'intérêt à 17h00 CET. Volatilité attendue sur les indices US. Positionnement défensif conseillé avant l'annonce.</div>
      <div class="alert-value" style="color:#f5c542;">⚡ Impact fort</div>
    </div>
  </div>
</section>

<div class="sep"></div>

<!-- HOW IT WORKS -->
<section class="section" id="how">
  <div class="section-label reveal">Fonctionnement</div>
  <div class="section-title reveal">Simple.<br><em>Automatique.</em></div>

  <div class="steps reveal">
    <div class="step">
      <div class="step-num">01</div>
      <div class="step-icon">📋</div>
      <h3>Choisissez vos actifs</h3>
      <p>Actions, crypto, ETF, indices. Ajoutez n'importe quel actif en quelques secondes depuis notre bibliothèque de 5000+ titres.</p>
    </div>
    <div class="step">
      <div class="step-num">02</div>
      <div class="step-icon">⚙️</div>
      <h3>Définissez vos conditions</h3>
      <p>Prix cible, variation %, RSI, croisement EMA, breakout de résistance — ou choisissez parmi nos stratégies prêtes à l'emploi.</p>
    </div>
    <div class="step">
      <div class="step-num">03</div>
      <div class="step-icon">🔍</div>
      <h3>Signal analyse en continu</h3>
      <p>Nos moteurs scannent les marchés en temps réel. Chaque tick, chaque variation, chaque annonce — rien n'est manqué.</p>
    </div>
    <div class="step">
      <div class="step-num">04</div>
      <div class="step-icon">🔔</div>
      <h3>Recevez vos alertes</h3>
      <p>Email, SMS, push, Discord ou Telegram. En moins d'une seconde après la détection du signal sur vos canaux préférés.</p>
    </div>
  </div>
</section>

<div class="sep"></div>

<!-- STRATEGIES -->
<section class="section" id="strategies">
  <div class="section-label reveal">Stratégies clé en main</div>
  <div class="section-title reveal">Démarrez en<br><em>un clic</em></div>

  <div class="strategies reveal">
    <div class="strategy-card">
      <div class="strategy-tag">🚀 Momentum</div>
      <h3>Breakout<br>Stratégie</h3>
      <p>Détection des cassures de résistance avec confirmation de volume. Idéal pour capter les mouvements forts au bon timing.</p>
      <div class="strategy-signals">
        <span class="signal-chip">Volume ×2</span>
        <span class="signal-chip">Cassure R</span>
        <span class="signal-chip">EMA croisement</span>
        <span class="signal-chip">RSI 45-65</span>
      </div>
    </div>
    <div class="strategy-card">
      <div class="strategy-tag">📈 Intermédiaire</div>
      <h3>Swing<br>Trading</h3>
      <p>Alertes sur rebonds techniques et retournements de tendance. Optimisé pour les positions 2 à 10 jours.</p>
      <div class="strategy-signals">
        <span class="signal-chip">Support/R</span>
        <span class="signal-chip">RSI 30/70</span>
        <span class="signal-chip">Divergence</span>
        <span class="signal-chip">Bougie J</span>
      </div>
    </div>
    <div class="strategy-card">
      <div class="strategy-tag">🏦 Long Terme</div>
      <h3>Investissement<br>Passif</h3>
      <p>Surveillance des tendances longues et des points d'entrée optimaux. Parfait pour les investisseurs patient et value.</p>
      <div class="strategy-signals">
        <span class="signal-chip">EMA 200</span>
        <span class="signal-chip">Résultats</span>
        <span class="signal-chip">Macro</span>
        <span class="signal-chip">Saisonnalité</span>
      </div>
    </div>
  </div>
</section>

<div class="sep"></div>

<!-- CHANNELS -->
<section class="section">
  <div class="section-label reveal">Canaux d'envoi</div>
  <div class="section-title reveal">Alertes sur<br><em>tous vos canaux</em></div>

  <div class="channels reveal">
    <div class="channel">
      <div class="channel-icon">📧</div>
      <div class="channel-name">Email</div>
      <div class="channel-desc">Alertes détaillées avec contexte et graphiques dans votre boîte mail.</div>
    </div>
    <div class="channel">
      <div class="channel-icon">📱</div>
      <div class="channel-name">SMS</div>
      <div class="channel-desc">Notification instantanée sur votre mobile. Jamais manqué un signal.</div>
    </div>
    <div class="channel">
      <div class="channel-icon">🔔</div>
      <div class="channel-name">Push</div>
      <div class="channel-desc">Notifications push sur iOS et Android via notre app mobile.</div>
    </div>
    <div class="channel">
      <div class="channel-icon">💬</div>
      <div class="channel-name">Telegram</div>
      <div class="channel-desc">Bot Telegram dédié pour recevoir et gérer vos alertes en chat.</div>
    </div>
    <div class="channel">
      <div class="channel-icon">🎮</div>
      <div class="channel-name">Discord</div>
      <div class="channel-desc">Intégration Discord pour vos serveurs de trading communautaires.</div>
    </div>
  </div>
</section>

<div class="sep"></div>

<!-- PRICING -->
<section class="section" id="pricing">
  <div class="section-label reveal">Tarification</div>
  <div class="section-title reveal">Commencez<br><em>gratuitement</em></div>

  <div class="pricing-grid reveal">
    <!-- Free -->
    <div class="plan">
      <div class="plan-name">Free</div>
      <div class="plan-price">
        <span class="currency">€</span>
        <span class="amount">0</span>
        <span class="period">/mois</span>
      </div>
      <p class="plan-desc">Pour découvrir Signal et tester vos premières alertes sans engagement.</p>
      <ul class="plan-features">
        <li><span class="feat-check">✓</span> 3 alertes simultanées</li>
        <li><span class="feat-check">✓</span> Alertes prix simples</li>
        <li><span class="feat-check">✓</span> Email uniquement</li>
        <li><span class="feat-check">✓</span> Actifs actions & crypto</li>
        <li class="disabled"><span class="feat-check">–</span> Alertes techniques</li>
        <li class="disabled"><span class="feat-check">–</span> Alertes intelligentes</li>
        <li class="disabled"><span class="feat-check">–</span> Multi-canaux</li>
      </ul>
      <button class="btn-plan btn-plan-outline">Commencer — Gratuit</button>
    </div>

    <!-- Pro (Featured) -->
    <div class="plan featured">
      <div class="plan-badge">Plus populaire</div>
      <div class="plan-name">Pro</div>
      <div class="plan-price">
        <span class="currency">€</span>
        <span class="amount">29</span>
        <span class="period">/mois</span>
      </div>
      <p class="plan-desc">Pour les traders actifs qui veulent toutes les alertes sans limite et tous les canaux.</p>
      <ul class="plan-features">
        <li><span class="feat-check">✓</span> Alertes illimitées</li>
        <li><span class="feat-check">✓</span> Alertes prix & variation</li>
        <li><span class="feat-check">✓</span> RSI, EMA, support/résistance</li>
        <li><span class="feat-check">✓</span> Alertes intelligentes</li>
        <li><span class="feat-check">✓</span> Email + SMS + Push</li>
        <li><span class="feat-check">✓</span> Telegram & Discord</li>
        <li><span class="feat-check">✓</span> Stratégies clé en main</li>
      </ul>
      <button class="btn-plan btn-plan-filled">Démarrer Pro</button>
    </div>

    <!-- Enterprise -->
    <div class="plan">
      <div class="plan-name">Expert</div>
      <div class="plan-price">
        <span class="currency">€</span>
        <span class="amount">49</span>
        <span class="period">/mois</span>
      </div>
      <p class="plan-desc">Pour les traders professionnels nécessitant des analyses avancées et une priorité absolue.</p>
      <ul class="plan-features">
        <li><span class="feat-check">✓</span> Tout le plan Pro</li>
        <li><span class="feat-check">✓</span> Alertes news macro</li>
        <li><span class="feat-check">✓</span> Résultats d'entreprises</li>
        <li><span class="feat-check">✓</span> Analyse IA personnalisée</li>
        <li><span class="feat-check">✓</span> Priorité d'exécution</li>
        <li><span class="feat-check">✓</span> API Access</li>
        <li><span class="feat-check">✓</span> Support prioritaire</li>
      </ul>
      <button class="btn-plan btn-plan-outline">Démarrer Expert</button>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section">
  <div class="section-label reveal">Prêt ?</div>
  <div class="section-title reveal">Ne manquez plus<br>aucun <em>signal</em></div>
  <p class="cta-desc reveal">
    Rejoignez 12 000 investisseurs qui utilisent Signal pour trader mieux, sans surveiller les marchés toute la journée.
  </p>
  <button class="btn-hero reveal" style="margin: 0 auto; display: flex;">
    Créer mon compte — Gratuit
    <span>→</span>
  </button>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">Signal</div>
  <ul class="footer-links">
    <li><a href="#">Produit</a></li>
    <li><a href="#">Tarifs</a></li>
    <li><a href="#">Blog</a></li>
    <li><a href="#">API</a></li>
    <li><a href="#">Mentions légales</a></li>
    <li><a href="#">Contact</a></li>
  </ul>
  <div class="footer-copy">© 2025 Signal. Tous droits réservés.</div>
</footer>

<script>
  // Reveal on scroll
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, { threshold: 0.1 });

  reveals.forEach(el => observer.observe(el));

  // Stagger children
  document.querySelectorAll('.alerts-demo, .steps, .pricing-grid, .strategies, .channels').forEach(parent => {
    [...parent.children].forEach((child, i) => {
      child.style.transitionDelay = `${i * 0.08}s`;
    });
  });

  // Simulate live ticker updates
  function randomizePrice(el, base, isUp) {
    const variance = (Math.random() - 0.5) * 2;
    const newPrice = (parseFloat(base) + variance).toFixed(2);
    return newPrice;
  }
</script>

</body>
</html>
